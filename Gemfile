source 'https://rubygems.org'

# TUTORIAL 2: REMEMBER TO CREATE A COMMON DEPENCIES FILE IN THE HOST ENGINE AND INCLUDED
# That way the host engines will have all the neccesary gems to run
eval(File.read(File.dirname(__FILE__) + '/../engines_structure/common_engines_structure_dependencies.rb'))
# TUTORIAL 3: INSTALL HOST ENGINE
gem 'engines_structure', path: '../engines_structure'
